import './main.tsx'

// This file is kept as a compatibility shim for tools that still reference main.jsx.
// The real entry point is src/main.tsx (TypeScript). Importing it here executes the same code.